// theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    text: {
      primary: '#000', 
      secondary: 'rgba(63,159,255,255)' // Set the primary text color to black
    },
    // Add more palette options as needed
    // ...
  }
});

export default theme;
